namespace Hyperpack.Models.CurseProxy {
    public enum DependencyType {
        EmbeddedLibrary,
        OptionalDependency,
        RequiredDependency,
        Tool,
        Incompatible,
        Include
    }
}